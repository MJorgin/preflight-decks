name: Deploy demo to Pages

# One-time bootstrap: the workflow's GITHUB_TOKEN can publish to a Pages site
# but cannot create one, so on a fresh fork (or after Pages is disabled) enable
# it first — Settings -> Pages -> Source: GitHub Actions, or:
#   gh api -X POST repos/<owner>/<repo>/pages -f build_type=workflow
# After that this workflow deploys the example deck on every push to main.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Stage the example deck as the site root
        run: |
          mkdir -p _site
          cp examples/preflight-decks-pitch.html _site/index.html
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
